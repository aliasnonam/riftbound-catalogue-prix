# Copy this file to android/keystore.properties and replace every placeholder.
# android/keystore.properties is ignored by Git and must remain local.
# Use forward slashes in Windows paths.
storeFile=E:/Secrets/Riftbound/riftbound-upload-key.jks
storePassword=CHANGE_ME
keyAlias=riftbound-upload
keyPassword=CHANGE_ME
